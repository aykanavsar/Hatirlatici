@model Hatirlatici.Models.Branch

<h1>Yeni Şube Ekle</h1>

<form method="post" action="@Url.Action("AddBranch", "Organization")">
    <div class="form-group">
        <label for="Name">Şube Adı:</label>
        <input type="text" id="Name" name="Name" class="form-control" required />
    </div>
    <div class="form-group">
        <label for="Description">Açıklama:</label>
        <textarea id="Description" name="Description" class="form-control"></textarea>
    </div>
    <div class="form-group">
        <label for="CompanyId">Şirket:</label>
        <select id="CompanyId" name="CompanyId" class="form-control">
            @foreach (var company in ViewBag.Companies)
            {
                <option value="@company.Id">@company.Name</option>
            }
        </select>
    </div>
    <button type="submit" class="btn btn-primary">Ekle</button> <!-- Ekle butonu burada -->
</form>
